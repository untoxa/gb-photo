#ifndef __MENU_EDIT_H_INCLUDE__
#define __MENU_EDIT_H_INCLUDE__

#include "menus.h"

uint8_t ShowEdit(const uint8_t * msg, uint8_t max_len) BANKED;
uint8_t Edit(const uint8_t * msg, uint8_t max_len);

#endif